--5月22日，重新连接postgres数据库，将原有的本地创建excel文件储存信息改为连接数据库，原excel相关代码副本保存在桌面“打车-文件形式-分页面.txt”，
修改后可正常进行登录操作，登陆后进入相应的用户身份页面，但仍会保留登录账号密码框。
注册功能出现问题，目测与参数输入有关。
5月23日目标：解决注册问题。
           优化登陆后页面简洁化。
           登陆后去除登录信息框
           理清后续功能页面设计逻辑

--5月23日，登录和注册功能完善 登陆后出现对应页面，但并不是出现一个新的页面，而是接在登录页面下方；
仍需解决问题：后续多页面逻辑设计  注册时无法向数据库中写入注册时填入的用户身份（已解决）




数据库表： trips 行程状态，有trip-id，order-id，status
          order订单表 有order-id，user-id，driver-id，订单状态status，起始点位置，行程距离 trip-distance
          reservation 同order，

postgres数据库中有用户表，表中有user_id，username，password，user_type（分别是用车人、管理员、专车司机），name（无用不管他），phone，address，city，driver表；
有driver表，表中有driver_id,name，phone，address，city，license_number（无用不用管），vehicle_plate（车牌号）。
order表，表中有order_id,user_id,driver_id,start_location,end_location,resavation_date,status,city,trip_distance.

--  1、 用户提交用车预约 并写入数据库表order中，包括 user-id，分配好的driver-id，从用户申请表读取的起始点城市，经过分析函数计算的行程距离，行程预计开始时间
用户的城市，status（默认为待确认）


--5月30日，利用sessionstate解决了不同页面文件间引用主页面user_id的问题，大致给出来专车司机页面的代码，但仍有问题，需要改进
   sidebar三个分页面需要仔细改进


--5月31日 基本完成专车司机页面全部设计

--6月1日基本完成用车人页面设计，并根据出现的问题改进专车司机页面漏洞

